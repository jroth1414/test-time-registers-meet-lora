# Plan progress

One line per finished chunk: date, chunk, commit, deviations.

- 2026-09-02 chunk 1 scaffold: 9b0f880 — torch 2.11.0+cu128 installed, CUDA confirmed on the RTX 5070 Ti; 11 tests pass; `tabulate` added to dependencies early (chunk 10 needs it); test for unknown config keys asserts `ConfigKeyError` instead of bare `Exception` to satisfy ruff B017.
- 2026-09-02 chunk 2 backbone: f37d758 (branch sdd/02-backbone) — subagent-driven; 21 backbone tests, 32 total. Deviations from the plan: `forward_tokens` now honours `model.grad_checkpointing` via timm `checkpoint_seq` (plan gap found in final review; plan text updated); `capture()` validates the target and fused-attention state before registering any hook via a module-level `_CAPTURE_TARGETS` map; dead `tt_reg_init` removed; extra guard tests. Real checkpoints verified by `scripts/smoke_backbone.py` (DINOv2-S/B with and without registers, CLIP ViT-B/16) and are now in the timm cache.
- 2026-09-02 task 6 reproduce_ttr: 2ce0246 — `scripts/reproduce_ttr.py` run on 4 real checkpoints against 64-image (200-image folder, `--calib 64`) ADE20K calibration set on the RTX 5070 Ti. Outlier fraction (before -> after, PASS/FAIL): `vit_small_patch14_dinov2.lvd142m` `--layer -2` 0.0147 -> 0.0013 PASS; `vit_base_patch14_dinov2.lvd142m` `--layer 8` 0.0175 -> 0.0035 PASS; `vit_base_patch16_clip_224.openai` `--img-size 224 --quantile 0.995 --max-neurons 200` 0.0390 -> 0.0002 PASS; `vit_small_patch14_reg4_dinov2.lvd142m` (baseline, default flags) 0.0000 -> 0.0000 n/a. Deviation from the brief's default flags: the default `--layer -1, --k 4.0, --quantile 0.999` under-detects on DINOv2 because the final-layer patch-norm distribution is not heavy-tailed relative to its own median there; per-layer norm quantiles showed the real outlier signature 1-4 blocks earlier, so `--layer` was moved earlier for both DINOv2 checkpoints. CLIP needed the opposite: kept `--layer -1` but loosened `--quantile`/`--max-neurons` to capture more of the relevant neurons. `src/ttr/registers.py` untouched. Full detail in `artifacts/register_neurons/README.md`.
- 2026-09-02 chunk 3 registers: 450df73 (branch sdd/03-registers) — subagent-driven; 21 register tests, 53 total. Deviations from the plan: `find_register_neurons` streams per-layer accumulators instead of buffering activations (13 GB at ViT-B/518 px) and uses population std on both paths; returns an empty map when the calibration set has no outliers or only outliers; `install_test_time_registers` validates layer/neuron ranges at install and raises if registers were removed after install; `save_register_neurons` writes a provenance `meta` block; `_resolve_layer` range-checks; the reproduction figure draws both the last layer and the deepest intervention layer with shared colour scale. Methodology notes for the paper are in the chunk ledger and README: the intervention zeroes register neurons on all patches (plan variant, faithful-variant ablation pending); outlier layer is model-specific (vits 10, vitb 8, clipb 11); DINOv2-B ratio 0.199 is borderline; H1 needs both fixed-tau and recalibrated fractions (now in chunk 7's plan).
- 2026-09-02 chunk 4 lora: ea4c565 (branch sdd/04-lora) — subagent-driven; 23 LoRA tests, 76 total. `LoRALinear` with per-slice targeting on timm's fused qkv, `apply_lora` (targets q/k/v/o, layer subset, atomic), `set_trainable` (frozen/lora/full), `count_params`, LoRA-only state dict with target guards. Deviation from the plan, made after the whole-branch review: adapters are standard per-matrix LoRA (independent A_g, B_g per targeted slice, 4rC per block for {q,v}) instead of the plan's shared-A draft (3rC); `qkv_out_index` became `qkv_out_slices`; `index_add` scatter with dtype cast verified under bf16 autocast and grad checkpointing. Chunk 7's plan gained a guard against `lora.enabled` outside lora mode and a full-mode backbone checkpoint.
- 2026-09-03 chunk 5 data: 4216b4c (branch sdd/05-data) — subagent-driven; 12 data tests, 88 total. `ttr.data`: v2 joint transforms (mask nearest, ignore 255 survives), `SegFolderDataset`, ADE20K pairs/label map with actionable mismatch errors, deterministic synthetic set, registry with validated names/splits; `docs/DATA.md`, `scripts/download_ade20k.ps1`. Deviations: `.gitignore` `data/` anchored to `/data/` (it had ignored `src/ttr/data/`); DATA.md states the runner overrides mean/std from `pretrained_cfg`. Verified on the real ADE20K (20210/2000) and with 2-worker DataLoaders on Windows. Chunk 7's plan gained a NaN-loss guard and a backbone/data `img_size` cross-check. Note for the paper: the fixed centre-crop protocol makes mIoU non-comparable to published full-image numbers.
- 2026-09-03 chunk 6 heads-metrics: 476c2b5 (branch sdd/06-heads-metrics) — subagent-driven; 17 tests, 105 total. `LinearHead` + `build_head` (mask head dispatched lazily to chunk 8), `ConfusionMeter` (mmseg-style NaN-mean mIoU, label-range guard added after final review), `image_miou`, `background_fraction` (torch.isin), `measure_throughput`. Per-image metric pass measured at ~4 s per 2000 ADE20K images. Chunk 7's plan gained autocast-wrapped throughput with a recorded dtype. Deviations from the plan: range guard, extra accumulation/edge-case tests, `zip(strict=True)`.
- 2026-09-03 chunk 7 runner: f4c53a1 (branch sdd/07-runner) — subagent-driven; 20 runner tests, 125 total. `ttr.run` (build_model, train/eval loops, run(), CLI), `scripts/make_factorial.py` (72 ADE20K cells), `scripts/run_sweep.ps1`, `configs/debug_synthetic.yaml`. Deviations after the whole-branch review: a run counts as complete only when metrics and diagnostics both exist (metrics written last); headline metric is last-epoch `final_miou` with no validation-set model selection; 5% warmup, grad clipping at 1.0, skipped-batch count; NaN-safe JSON; `DataCfg.diag_images=512`; recalibration skipped on frozen arms; `register_neuron_path` mandatory for test-time registers on real checkpoints. Sanity cells (5 epochs, DINOv2-S frozen, 224 px) under `results/sanity/`: none 0.4034 / test_time 0.4022 / trained 0.4044 mIoU; outlier fraction 0.0050 / 0.0010 / 0.0026. Register-neuron maps re-detected at 224 px (`artifacts/res224/`): with the 518 px flags DINOv2 failed the 0.2 bar (0.43, 0.23); one recorded tuning round (30 trials in the README) gives S 0.144 and B 0.025, CLIP 0.004; `OUTLIER_LAYER` vits set to 9 to match. ViT-B full fine-tune fits at batch 16. Frozen ViT-S cell: 75 s/epoch, 881 img/s bf16.
- 2026-09-15 chunk 8 mask-head: 8f91d8f (branch sdd/08-mask-head) — subagent-driven; 7 mask-head tests, 134 total. `ttr.mask_head.MaskHead` (MaskFormer-lite): one query per class as a scaled `nn.Parameter`, two-layer `nn.TransformerDecoder` with a final LayerNorm over projected patch tokens plus a learned 32x32 positional table interpolated to the grid, pixel branch 1x1 conv -> 4x bilinear upsample -> 3x3 conv, dot-product mask logits; fixed query-to-class assignment so the loss stays per-pixel cross-entropy and the runner is unchanged. `HeadCfg` gains `num_layers`, `heads`, `upsample`; `scripts/make_factorial.py --head mask` writes 72 `__mask` cells (144 ADE20K configs). Deviations after the whole-branch review: the upsample moved between the two convs (as listed it commuted with the einsum), decoder LayerNorm, positional memory (the listed head was permutation-invariant over patches), Parameter queries, no bias on the last mask-embed linear. Honest name is MaskFormer-lite; it omits Hungarian matching, per-query classification, dice/focal losses, a multi-scale pixel decoder, masked attention and deep supervision, so the paper must not call it Mask2Former-style (the submitted proposal keeps that phrase). Sanity cell `results/sanity/ade20k__vits__frozen__none__s0__mask` (frozen DINOv2-S, 5 epochs, 224 px): final mIoU 0.3732 (0.265 -> 0.373, still climbing), pixel acc 0.768, 2.80M head params, 45 s/epoch, 1565 img/s bf16; the linear probe at the same budget scores 0.4034 and the pre-fix head scored 0.3535. The mask head converges slower than the probe, so heads are compared only at the full 10-epoch schedule. Carried forward: chunk 9 Task 4 splits AdamW into decay/no-decay groups (1-D params and the query table); chunk 10 should forward `head_params` and label `trainable_params` as backbone-only (it is 0 on every frozen arm by design).
- 2026-09-15 chunk 9 cityscapes-lars: b9c2a22 (branch sdd/09-cityscapes-lars) — subagent-driven; 19 new tests, 153 total. `ttr.data.cityscapes` (34 raw ids -> 19 train ids by LUT, `cityscapes_pairs`, background ids road/wall/sky), `ttr.data.lars` (obstacle 0, water 1, sky 2, everything else 255, ndim guard; `scripts/inspect_lars.py` confirms the raw values after download), `ttr.metrics_lars` (`boundary_f1` water-edge proxy at 10 px tolerance, `pixel_f1` obstacle proxy; per-image means over the val split; boundary pixels within one pixel of void dropped), `evaluate(..., extra_fn)` hook writing `extra_*` to metrics.json and `val_*` per-epoch columns to log.csv (LaRS only), `patch_norm_quantiles` with `norm_ratio_p999` / `norm_ratio_max` diagnostics, `build_optimizer` with AdamW decay / no-decay groups (1-D params, `pos_embed`/`cls_token`/`reg_token`, mask-head queries). Cityscapes and LaRS cells run at 448 px, batch 8: 72 linear-head configs each under `configs/cityscapes` and `configs/lars`, pointed at 448 px register-neuron maps in `artifacts/res448/` (224 px flags, all PASS: vits ratio 0.138, vitb 0.032, clipb 0.023; README table). Neither dataset is on disk (registration required); loaders are tested on fake fixtures and `docs/DATA.md` records the download layout, the inspector step, and that the LaRS numbers are proxies for the official toolkit. Deviations after the whole-branch review: ignore-aware boundary F1 (a void blob inside water had inverted the metric ordering), per-epoch LaRS metrics logged, token embeddings excluded from weight decay, empty-loader and 2^24 guards on the quantile diagnostic. Memory probe at 448 px batch 8 on synthetic data: vits full 1.95 GiB, clipb full 3.60 GiB, vitb full (checkpointed) 1.99 GiB peak, so the grad_checkpoint predicate stays vitb-full only. Sanity of the changed runner on ADE20K (scratch, not committed): vits frozen 5 epochs 0.4033 (chunk 7: 0.4034), vits lora 1 epoch 0.4089, frozen norm_ratio_p999 1.79. Notes for chunk 10: `trainable_params` counts backbone parameters only (forward `head_params` too); `bg_fraction` depends on the labels alone, so adding building to the Cityscapes background list is an analysis-time sensitivity check, not a re-run; mask-head variants of the new datasets come from `scripts/make_factorial.py --head mask`.
- 2026-09-15 chunk 10 analysis: 7ad509b (branch sdd/10-analysis) — subagent-driven; 27 analysis tests, 180 total. `ttr.analysis`: `collect` (one row per `results/<run_id>/`; forwards final_miou, best_miou, head_params, backbone-only trainable_params, the recalibrated and last-layer outlier fractions, norm_ratio_p999 / norm_ratio_max, throughput_dtype and every extra_* key; accepts only factorial run ids plus `__mask` and skips sanity/, sweep_logs/ and anything else with a message), `summarize` (mean and std with ddof 1, n, per dataset / fam / mode / registers / head; carries backbone_trainable_params and head_params), `paired` (two-sided paired t-test over seed-matched final_miou with a 95% CI; zero variance reports t = inf and p = n/a instead of 0), `h1` (outlier ratio lora/frozen within 1 +- 0.2 AND lora-none below lora-trained at p < 0.05; both components are columns), `h2` (closure defined only when trained > none; H2 needs closure >= 0.5 and a positive test-time gain; a tt-vs-trained test and extra_params_tt substantiate the zero-parameter claim), `h3` (Spearman over distinct validation images with the gain averaged over seeds; H3_rank_ok and lars_is_max need at least two datasets; the two per-image files must agree on bg_fraction), `h3_backbone_ordering` (CLIP gain against DINOv2 per dataset and head), `to_markdown` with a note under each table, and `python -m ttr.analysis --results results --out paper/tables` writing summary, h1, h2, h3, h3_backbones as .md and .csv plus miou_by_cell.png (one panel per head and dataset, bars grouped by mode, coloured by register arm). Deviations after the whole-branch review: H1 gained the significance clause (the listed verdict used the sign of the gap only), H2 gained the sign guard (the listed closure flipped sign when the registered backbone lost), H3 moved from pooled image-seed pairs to per-image averages, and hypothesis grouping includes the head so mask cells never pool with linear cells. Not yet run on real cells: no factorial cell has finished (the ADE20K sweep waits behind another GPU job). Caveats for the paper pass: never quote a one-seed H2 (no n guard); lars_is_max is group-level, read it with n_images > 0; hyper-parameter sweeps write to results/sweeps/ (CLAUDE.md rule added this chunk); with three seeds a paired test needs |t| > 4.3 for p < 0.05, so the tables carry the CI and both H1 components.
- 2026-09-15 ADE20K frozen sweep: 2292855 — 24 linear-head cells (3 backbones x register arms x 3 seeds, 10 epochs, 224 px), none failed, 13-15 min per cell; committed under `results/ade20k__*__frozen__*`; `python -m ttr.analysis` aggregates them without error. final_miou mean (std): clipb none 0.3516 (0.0009), clipb test_time 0.3449 (0.0006); vitb none 0.4357 (0.0007), test_time 0.4375 (0.0006), trained 0.4411 (0.0004); vits none 0.4060 (0.0017), test_time 0.4032 (0.0021), trained 0.4133 (0.0001). Test-time registers remove the outliers on every frozen backbone (outlier fraction 0.0401 -> 0.0006 clipb, 0.0190 -> 0.0003 vitb, 0.0034 -> 0.0005 vits; norm_ratio_p999 6.37 -> 1.46, 58.8 -> 1.34, 1.79 -> 1.37) at a 25% throughput cost, while the frozen linear-probe mIoU moves by less than a point in either direction; trained registers add 0.5 to 0.7 points. The H2 claim concerns the LoRA arm, whose sweep started 19:33.
- 2026-09-16 ADE20K lora sweep: 768a7f2 — 24 linear-head LoRA cells (r 8, q/v, 10 epochs, 224 px), none failed, 15-21 min per cell; committed under `results/ade20k__*__lora__*`. final_miou mean over 3 seeds: clipb none 0.4111 / test_time 0.4117; vitb none 0.4795 / test_time 0.4802 / trained 0.4835; vits none 0.4413 / test_time 0.4394 / trained 0.4397. First `python -m ttr.analysis` verdicts (48 cells, linear probe): H1 False for vitb and vits (the fixed-tau outlier fraction under LoRA is 0.29x and 0.90x the frozen value, so vitb trips the 20% clause; the lora-none vs lora-trained gap is -0.4 and +0.2 points with p 0.31 and 0.25, so LoRA matches the registered backbone within seed noise); clipb H1 n/a (no trained arm, ratio 0.72). H2 False for vitb (closure 0.18 of a 0.4-point gap, tt gain +0.07 points, p 0.58), n/a for vits (trained arm below lora-none by 0.2 points) and clipb (tt gain +0.06 points, p 0.88); extra_params_tt = 0 everywhere. Observation for the paper: LoRA on q/v removes most tokens above the frozen tau on ViT-B (0.0190 -> 0.0056, recalibrated 0.0045) yet the scale-free norm_ratio_p999 stays at 41 (frozen 59), so a few extreme tokens survive while the tau-relative fraction falls; test-time registers still normalise the ratio to 1.3 on every backbone, at no mIoU change larger than 0.2 points on this dataset at 224 px with a linear probe. The full fine-tune filter started 02:47.
