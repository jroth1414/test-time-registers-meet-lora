# Plan progress

One line per finished chunk: date, chunk, commit, deviations.

- 2026-09-02 chunk 1 scaffold: 9b0f880 — torch 2.11.0+cu128 installed, CUDA confirmed on the RTX 5070 Ti; 11 tests pass; `tabulate` added to dependencies early (chunk 10 needs it); test for unknown config keys asserts `ConfigKeyError` instead of bare `Exception` to satisfy ruff B017.
- 2026-09-02 chunk 2 backbone: f37d758 (branch sdd/02-backbone) — subagent-driven; 21 backbone tests, 32 total. Deviations from the plan: `forward_tokens` now honours `model.grad_checkpointing` via timm `checkpoint_seq` (plan gap found in final review; plan text updated); `capture()` validates the target and fused-attention state before registering any hook via a module-level `_CAPTURE_TARGETS` map; dead `tt_reg_init` removed; extra guard tests. Real checkpoints verified by `scripts/smoke_backbone.py` (DINOv2-S/B with and without registers, CLIP ViT-B/16) and are now in the timm cache.
- 2026-09-02 task 6 reproduce_ttr: 2ce0246 — `scripts/reproduce_ttr.py` run on 4 real checkpoints against 64-image (200-image folder, `--calib 64`) ADE20K calibration set on the RTX 5070 Ti. Outlier fraction (before -> after, PASS/FAIL): `vit_small_patch14_dinov2.lvd142m` `--layer -2` 0.0147 -> 0.0013 PASS; `vit_base_patch14_dinov2.lvd142m` `--layer 8` 0.0175 -> 0.0035 PASS; `vit_base_patch16_clip_224.openai` `--img-size 224 --quantile 0.995 --max-neurons 200` 0.0390 -> 0.0002 PASS; `vit_small_patch14_reg4_dinov2.lvd142m` (baseline, default flags) 0.0000 -> 0.0000 n/a. Deviation from the brief's default flags: the default `--layer -1, --k 4.0, --quantile 0.999` under-detects on DINOv2 because the final-layer patch-norm distribution is not heavy-tailed relative to its own median there; per-layer norm quantiles showed the real outlier signature 1-4 blocks earlier, so `--layer` was moved earlier for both DINOv2 checkpoints. CLIP needed the opposite: kept `--layer -1` but loosened `--quantile`/`--max-neurons` to capture more of the relevant neurons. `src/ttr/registers.py` untouched. Full detail in `artifacts/register_neurons/README.md`.
- 2026-09-02 chunk 3 registers: 450df73 (branch sdd/03-registers) — subagent-driven; 21 register tests, 53 total. Deviations from the plan: `find_register_neurons` streams per-layer accumulators instead of buffering activations (13 GB at ViT-B/518 px) and uses population std on both paths; returns an empty map when the calibration set has no outliers or only outliers; `install_test_time_registers` validates layer/neuron ranges at install and raises if registers were removed after install; `save_register_neurons` writes a provenance `meta` block; `_resolve_layer` range-checks; the reproduction figure draws both the last layer and the deepest intervention layer with shared colour scale. Methodology notes for the paper are in the chunk ledger and README: the intervention zeroes register neurons on all patches (plan variant, faithful-variant ablation pending); outlier layer is model-specific (vits 10, vitb 8, clipb 11); DINOv2-B ratio 0.199 is borderline; H1 needs both fixed-tau and recalibrated fractions (now in chunk 7's plan).
- 2026-09-02 chunk 4 lora: ea4c565 (branch sdd/04-lora) — subagent-driven; 23 LoRA tests, 76 total. `LoRALinear` with per-slice targeting on timm's fused qkv, `apply_lora` (targets q/k/v/o, layer subset, atomic), `set_trainable` (frozen/lora/full), `count_params`, LoRA-only state dict with target guards. Deviation from the plan, made after the whole-branch review: adapters are standard per-matrix LoRA (independent A_g, B_g per targeted slice, 4rC per block for {q,v}) instead of the plan's shared-A draft (3rC); `qkv_out_index` became `qkv_out_slices`; `index_add` scatter with dtype cast verified under bf16 autocast and grad checkpointing. Chunk 7's plan gained a guard against `lora.enabled` outside lora mode and a full-mode backbone checkpoint.
